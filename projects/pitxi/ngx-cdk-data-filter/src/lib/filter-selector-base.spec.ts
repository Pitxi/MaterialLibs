import { DataFilterSelector } from './filter-selector-base';

describe('FilterSelectorBase', () => {
  it('should create an instance', () => {
    expect(new DataFilterSelector()).toBeTruthy();
  });
});
