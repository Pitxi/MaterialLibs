import { CdkDataFilterDirective } from './cdk-data-filter.directive';

describe('CdkDataFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new CdkDataFilterDirective();
    expect(directive).toBeTruthy();
  });
});
