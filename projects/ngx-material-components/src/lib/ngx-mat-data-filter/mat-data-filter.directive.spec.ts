import { MatDataFilterDirective } from './mat-data-filter.directive';

describe('MatDataFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new MatDataFilterDirective();
    expect(directive).toBeTruthy();
  });
});
