import { DataFilterConfiguration } from './data-filter-configuration';

describe('DataFilterConfiguration', () => {
  it('should create an instance', () => {
    expect(new DataFilterConfiguration()).toBeTruthy();
  });
});
